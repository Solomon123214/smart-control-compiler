[project]
name = "smart-control-compiler"
authors = ["Smart Control Team"]
description = "A decentralized platform for managing creative challenges with transparent governance"
telemetry = false
requirements = []
[contracts.challenge-control]
path = "contracts/challenge-control.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
